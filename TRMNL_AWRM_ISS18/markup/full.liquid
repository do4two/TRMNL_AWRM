<div class="layout layout--col">
  <div class="x-canvas x-full">
    <div class="x-header">
      <span class="x-title">Müllabfuhr</span>
      <span class="x-meta">{{ address }} · Stand {{ generated_at_human }}</span>
    </div>

    {% if has_pickups %}
    {% assign n = next %}
    <div class="x-content">
      {% comment %} ---------- Hero: nächste Abfuhr ---------- {% endcomment %}
      <div class="awrm-hero{% if n.is_soon %} awrm-hero--soon{% endif %}">
        <div class="awrm-hero-top">
          <span class="awrm-eyebrow">Nächste Abfuhr</span>
          {% if n.badge != "" %}<span class="awrm-when">{{ n.badge }}</span>{% endif %}
        </div>
        <div class="awrm-date">{{ n.weekday }}, {{ n.date_human }}</div>
        <div class="awrm-rel">{{ n.relative }}</div>
        <div class="awrm-items">
          {% for it in n.items %}
          <div class="awrm-item">
            <span class="awrm-tag">{{ it.tag }}</span>
            <span class="awrm-item-name">{{ it.name }}</span>
          </div>
          {% endfor %}
        </div>
      </div>

      {% comment %} ---------- Liste: Danach ---------- {% endcomment %}
      <div class="awrm-list">
        <span class="awrm-list-label">Danach</span>
        <div class="awrm-rows">
          {% for u in upcoming offset: 1 limit: 5 %}
          <div class="awrm-row">
            <div class="awrm-row-date">
              <div class="awrm-row-dm">{{ u.day }}.{{ u.month }}</div>
              <div class="awrm-row-wd">{{ u.weekday_short }}</div>
            </div>
            <div class="awrm-row-items">
              {{ u.items_text }}{% if u.badge != "" %}<span class="awrm-mini-when">{{ u.badge }}</span>{% endif %}
            </div>
          </div>
          {% endfor %}
        </div>
      </div>
    </div>
    {% else %}
    <div class="x-content">
      <div class="awrm-empty">
        <div class="awrm-empty-title">Keine Termine</div>
        <div class="awrm-empty-sub">{{ error }}</div>
      </div>
    </div>
    {% endif %}
  </div>
</div>
